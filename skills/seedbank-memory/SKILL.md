---
name: Seedbank Memory
description: Basic CRUD operations for the Seedbank API
---

# Seedbank Memory

This skill provides a simple bash-based CLI to interact with the core Seedbank backend, specifically handling the creation and searching of generalized AI context structures.

## Scripts
- `seedbank-memory.sh`:
  - Provide a command line interface wrap around Seedbank's `POST /agent-contexts` and `GET /search` functionality.

## Usage Guidelines
- Useful for generalized data logging or testing backend connectivity.
