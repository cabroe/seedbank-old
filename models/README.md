# GTE-Small model

The model file `gte-small.gtemodel` is not committed (too large). Generate it once:

```bash
# From project root
./scripts/setup-model.sh
```

Or see the main [README](../README.md) for manual steps.
